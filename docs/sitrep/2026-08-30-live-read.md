# Sitrep — first live fabric session, 2026-08-30

The source of record for the `live-read-verified` entries in
`src/pyecsdwan/_evidence/ledger.json`. Everything here was observed, not
inferred; where something is inferred it says so.

## Environment

| | |
|---|---|
| Orchestrator | **9.7.0.43282** |
| ECOS | **9.7.0.0_109184** (uniform across the fleet) |
| Auth | **api-key**, from `ECSDWAN_API_KEY` |
| Fabric | 6 × EC-V across sites S1/S2/S3 |
| Reachable | 4 — `S3-ecv-01` and `S3-ecv-02` were in maintenance (`state: 2`) |
| Date | 2026-08-30 |

## What was run

A read sweep over every curated kind. Per kind, per reachable appliance:

1. `list_refs()`
2. `fetch()`
3. `normalize()`, then `normalize(normalize(x)) == normalize(x)`
4. a canonical state diffed against **itself**, asserted empty

Step 4 is the read-only half of the level-4 no-op proof: it shows the resource
does not manufacture drift out of its own output. It is *not* level 4, which
requires actually committing unchanged state back.

**Result: 41 curated kinds, 0 idempotency failures, 0 phantom drift.**

38 kinds are promoted to `live-read-verified`. Three are not:
`appliance/vrrp`, `security-policy` and `ip-service-group` returned zero refs
because nothing of that kind is configured here — there was nothing to read,
which is not the same as reading it successfully.

## Level 4 — no-op round trip

Run by the documented procedure in `docs/live-validation.md`: `show` the
current state, `load` it back, `diff` (must be empty), `commit`, `diff` again.
Non-destructive by construction — an empty plan writes nothing, and no
transaction was journaled by any of it.

| Kind | Shape it exercises | diff → commit → diff |
|---|---|---|
| `appliance/banners` | simple settings dict | empty → no changes → empty |
| `appliance/bgp` | nested system + peer-keyed neighbours | empty → no changes → empty |
| `appliance/qos-map` | prio-keyed map (what L2 ownership reasons about) | empty → no changes → empty |
| `appliance/snmp` | settings dict with communities | empty → no changes → empty |
| `interface-labels` | orchestrator-scope, id-keyed tables | empty → no changes → empty |

Chosen for structural variety rather than convenience: a flat dict, a nested
object with a keyed sub-table, a priority-keyed map, and an orchestrator-scope
resource. If canonicalization were going to invent drift, these are the shapes
it would do it in.

**A programmatic equivalent passed for 33 write-capable kinds with zero phantom
drift** — the same computation (`normalize` → `canonicalize_desired` → `diff`)
without the CLI around it. Those are *not* promoted. The ledger records levels
by the procedure that was actually run, and a level claimed by a different
route is a weaker claim wearing the same label.

## Level 5 — a real change, verified and rolled back

`appliance/banners` on `S1-ecv-02` (13.NE), by the documented sequence.

| Step | Result |
|---|---|
| `set banners config motd "..."` | staged |
| `commit --override-template --confirm-minutes 10` | applied, watchdog armed, `13.NE: Saved change on appliance successfully` |
| read the appliance | new motd present, **`issue` untouched** |
| `hasUnsavedChanges` | `False` — reached flash |
| `confirm` | CONFIRMED, watchdog stopped |
| `rollback` | restored, new transaction CONFIRMED |
| read the appliance | **byte-identical to the pre-change baseline** |
| `hasUnsavedChanges` | `False` again |

`issue` staying untouched matters: `banners` is a full-object replace, so the
write carried both fields and only one changed. That is the property a
full-object POST is most likely to get wrong.

The override was needed and was *correct*. Unlike BGP peers, the `banners`
template body **is** the banner text, so this is ownership working rather than
over-scoping — and the next template push would restore it regardless.

### Two findings from the attempt

**The poller fix is verified live.** The first attempt failed on an unrelated
error, and the engine auto-reverted. Where the same path earlier reported
`UNKNOWN` and `REVERT_FAILED`, it now reported:

```
per-appliance results (SUCCESS):  13.NE: Saved change on appliance successfully
transaction: ... [REVERTED]
```

The fabric was verified byte-identical to baseline after that failure too, so
the failure path and the success path both restore correctly.

**The banner endpoint rejects non-ASCII.** An em-dash produced
`400: Non-ASCII character (—) found at index 80`. Worth knowing before writing
banner text from a document that has been through a word processor.

## Level 5 — `appliance/bgp`, and the two defects it took to get there

The operation this session was originally asked for: add a placeholder BGP
peer, then remove it. It needed **no `--override-template`** — the ownership
rework's whole point, now proven on a real write rather than a plan. The
journal records it:

```
APPLY_START  ownership: "unowned"
             reason: "...governs appliance/bgp, but not the fields this change touches"
```

Peer `192.0.2.77` (RFC 5737 documentation space) added under a confirm window,
verified present with both working EVPN peers byte-identical, confirmed,
rolled back, final state byte-identical to baseline for both the neighbour
table and the system object, `hasUnsavedChanges` False throughout.

**Neither defect below is visible to any read-only check.** With no change
pending, both copies agree and both round trips are clean — which is why the
level-3 and level-4 sweeps passed `appliance/bgp` and a live write still
failed twice.

### 1. The canonical state held neighbours twice

`bgp/config/system` echoes a `neighbor` map mirroring the standalone neighbour
table, and `normalize()` kept it. So a changeset that added a peer produced a
desired state whose `system.neighbor` was stale *by construction*, and
post-apply verify reported:

```
remove  system.neighbor.192.0.2.77   desired=null  actual={...}
```

Fixed: `normalize()` drops the mirror, `_write` re-derives it from the
neighbour table being written. The wire payload is unchanged in shape; what
changed is that it is now current rather than whatever the last read carried.

### 2. A peer create must carry a complete object

With the mirror fixed, verify still failed on three fields:

```
remove  neighbors.192.0.2.77.password        desired=null  actual=""
remove  neighbors.192.0.2.77.rtmap_inbound   desired=null  actual=""
remove  neighbors.192.0.2.77.rtmap_outbound  desired=null  actual=""
```

The appliance defaults them on create; strict verify then sees fields it did
not ask for. Staging a complete 17-field peer passes.

This is **not** fixed, and should not be fixed here: it is exactly D7 in
`specs/003-declarative-apply` — "the resource materializes a complete target
from current state and the declaration; it must not blindly post a partial
document" — and building that per resource is T8. Recorded so T8 has a live
worked example rather than a hypothesis.

## What this does not claim

**Two write paths have been verified** — `appliance/banners` and
`appliance/bgp`, above. The other 39 have not, level 4 included. A no-op round trip
proves the resource round-trips its own state without inventing drift; because
the plan is empty, `commit` writes nothing, so it says nothing about whether
the write path works. Level 5 is the floor for that, and exactly two resources have reached it.

An earlier BGP write was executed and reverted
(below), and it did not produce level-4 evidence: the poller could not classify
the response, so nothing about the write was confirmed at the time. Every write
in this repository remains code that has not met a fabric.

## Defects found

**The save-changes poller did not recognise 9.7's success shape.** A BGP
neighbour write reached the fabric and was saved; the poller saw
`taskStatus='COMPLETED' result='Saved change on appliance successfully'`,
matched no allowlisted shape, and returned UNKNOWN. The engine treated the
apply as failed and auto-reverted — and the revert hit the *same* unrecognised
shape, reporting `REVERT_FAILED`. The fabric was correct throughout: both the
apply and the revert really happened, verified byte-identical against a
pre-change baseline. Fixed, with the shape recorded in
`docs/research/job-shapes.md`.

**Template ownership was wrong in both directions.** Full analysis in
`docs/research/live-ownership-2026-08-30.md`; the model and the owner's
decisions are `specs/004-ownership-granularity/spec.md`. In short: five kinds
named template sections this Orchestrator has never heard of and therefore
could never detect an owner (`optimization-map` looked for `optimizationMaps`
where the real section is `optmap`, which demonstrably pushes); and ownership
was keyed by resource kind where real authority is per field and per priority
band, so locally significant changes — BGP peers — were refused.

**A cache-key collision I introduced during the rework.** `ownership` cached
full template-group bodies under the key `Resolver.template_groups()` already
owned for a list of names. `template-group.list_refs` then built refs whose
name was an entire group object. Only the sweep hit it, because it needs both
callers in one process in one order.

**Three live-only tests had never executed anywhere.** They called
`config.load_settings()`, which does not exist, and were gated on
`ECSDWAN_ORCH_URL` — so no mock-only host ever ran them. They also fetched
every ref `list_refs` returns, including unreachable appliances.

## What the tool got right

`drift` degrades correctly on an incomplete fabric. With two appliances
unreachable it reported `unreadable: 2` and refused to claim "no drift":

```
drift: 0  in-sync: 0  undeclared: 4  unreadable: 2
incomplete: 2 instance(s)/scope(s) could not be compared, so "no drift" is not
a claim this run can make                                          exit 8
```

That is spec 003's T9 criterion — "list failure is incomplete" — satisfied with
live evidence rather than mock evidence.

## Re-reading this

The sweep is `/tmp/ec-live/sweep.py` in the session that produced it; it is
short enough to re-derive from this description and depends on nothing but the
registry and a configured client. The captured template bodies and appliance
payloads were not committed — they carry lab addressing and would age into
misleading fixtures. `docs/research/live-ownership-2026-08-30.md` quotes the
specific values every claim rests on.
