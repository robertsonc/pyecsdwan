"""Generated client bindings, one module per spec operation.

Each module exposes the operation's constants (``SCOPE``, ``METHOD``,
``PATH``/``ECOS_PATH``, ``EXPECTED_STATUS``) and a single function that calls
through :class:`pyecsdwan.client.OrchClient` -- ``request`` for orchestrator
paths, ``appliance_request`` for appliance (ECOS) paths.
"""
