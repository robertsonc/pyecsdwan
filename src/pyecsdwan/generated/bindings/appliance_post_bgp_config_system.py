"""Typed client binding for ``appliance POST /bgp/config/system``.

Machine-generated by ``tools/gen_models.py`` (issue #26) -- do not edit.
Regenerate with::

    python tools/gen_models.py \
            --scope appliance --method POST \
            --path /bgp/config/system

Source: specs/appliance-openapi-7.2.0.json (spec version 7.2.0), operationId
``BgpSystemConfig351565``.

Set appliance's BGP system level configuration data.
"""

from __future__ import annotations

from collections.abc import Mapping
from typing import Any

from pyecsdwan.client import OrchClient
from pyecsdwan.generated._base import dump_body
from pyecsdwan.generated.models.appliance_post_bgp_config_system import (
    AppliancePostBgpConfigSystemRequest,
)

#: Payload shapes this binding accepts; a raw mapping or list is always
#: allowed, because the 7.2.0 baseline is sometimes wrong about a shape.
RequestBody = AppliancePostBgpConfigSystemRequest | Mapping[str, Any] | list[Any]

SCOPE = "appliance"
METHOD = "POST"
#: Relative on purpose: OrchClient.appliance_request passes this as the
#: proxy's ``url`` parameter, where a leading slash would resolve to
#: ``rest/json//<path>`` on the appliance.
ECOS_PATH = "bgp/config/system"
EXPECTED_STATUS = (200,)

__all__ = ["appliance_post_bgp_config_system"]


def appliance_post_bgp_config_system(
    client: OrchClient,
    ne_pk: str,
    body: RequestBody,
    *,
    expected: tuple[int, ...] = EXPECTED_STATUS,
) -> Any:
    """Set appliance's BGP system level configuration data.

    The spec gives no usable response schema, so parsed JSON is returned as-is.
    """
    return client.appliance_request(
        METHOD,
        ne_pk,
        ECOS_PATH,
        json_body=dump_body(body),
        expected=expected,
    )
