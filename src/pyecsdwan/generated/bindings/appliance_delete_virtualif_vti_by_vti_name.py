"""Typed client binding for ``appliance DELETE /virtualif/vti/{vtiName}``.

Machine-generated by ``tools/gen_models.py`` (issue #26) -- do not edit.
Regenerate with::

    python tools/gen_models.py \
            --scope appliance --method DELETE \
            --path '/virtualif/vti/{vtiName}'

Source: specs/appliance-openapi-7.2.0.json (spec version 7.2.0), operationId ``vtiDelete802597``.

Delete vti interface {vtiName}.
"""

from __future__ import annotations

from typing import Any

from pyecsdwan.client import OrchClient
from pyecsdwan.generated._base import format_path

SCOPE = "appliance"
METHOD = "DELETE"
#: Relative on purpose: OrchClient.appliance_request passes this as the
#: proxy's ``url`` parameter, where a leading slash would resolve to
#: ``rest/json//<path>`` on the appliance.
#: ``{placeholder}`` segments are substituted by the binding below.
ECOS_PATH = "virtualif/vti/{vtiName}"
EXPECTED_STATUS = (200,)

__all__ = ["appliance_delete_virtualif_vti_by_vti_name"]


def appliance_delete_virtualif_vti_by_vti_name(
    client: OrchClient,
    ne_pk: str,
    vti_name: str,
    *,
    expected: tuple[int, ...] = EXPECTED_STATUS,
) -> Any:
    """Delete vti interface {vtiName}.

    This API deletes the vti interface with name {vtiName}

    The spec gives no usable response schema, so parsed JSON is returned as-is.
    """
    path = format_path(ECOS_PATH, {"vtiName": vti_name})
    return client.appliance_request(METHOD, ne_pk, path, expected=expected)
