"""Typed client binding for ``orchestrator POST /gms/treeConfig``.

Machine-generated by ``tools/gen_models.py`` (issue #26) -- do not edit.
Regenerate with::

    python tools/gen_models.py \
            --scope orchestrator --method POST \
            --path /gms/treeConfig

Source: specs/orchestrator-openapi-7.2.0.json (spec version 7.2.0), operationId
``gmsTreeConfigByPost1002``.

Update the user's tree view configuration Settings, including sorting, grouping, visible parameters,
and counts.
"""

from __future__ import annotations

from collections.abc import Mapping
from typing import Any

from pyecsdwan.client import OrchClient
from pyecsdwan.generated._base import dump_body
from pyecsdwan.generated.models.orchestrator_post_gms_tree_config import (
    OrchestratorPostGmsTreeConfigRequest,
)

#: Payload shapes this binding accepts; a raw mapping or list is always
#: allowed, because the 7.2.0 baseline is sometimes wrong about a shape.
RequestBody = OrchestratorPostGmsTreeConfigRequest | Mapping[str, Any] | list[Any]

SCOPE = "orchestrator"
METHOD = "POST"
PATH = "/gms/treeConfig"
EXPECTED_STATUS = (201,)

__all__ = ["orchestrator_post_gms_tree_config"]


def orchestrator_post_gms_tree_config(
    client: OrchClient,
    body: RequestBody,
    *,
    params: dict[str, Any] | None = None,
    expected: tuple[int, ...] = EXPECTED_STATUS,
) -> Any:
    """Update the user's tree view configuration Settings, including sorting, grouping, visible
    parameters, and counts.

    The spec gives no usable response schema, so parsed JSON is returned as-is.
    """
    return client.request(METHOD, PATH, json_body=dump_body(body), params=params, expected=expected)
